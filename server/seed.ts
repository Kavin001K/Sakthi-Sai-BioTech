
// This file is no longer used for seeding as the project currently uses in-memory storage.
// Initial data is loaded automatically in server/storage.ts upon server start.

console.log("🌱 Data seeding is handled automatically by the in-memory storage in server/storage.ts");
console.log("✅ No manual seeding required.");
